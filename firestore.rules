rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public access to all documents for this simple chat app
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
